<section class="content">
  <h3>Your brews:</h3>
  {{#if brews}}
  Click one to view or click new brew to record a new cup
  {{else}}
  You don't have any brews yet! Click new brew to create one.
  {{/if}}
  {{#each brews}}
  <ul>
    <a href="/brews/{{../userId}}/{{_id}}">
      <li>Created at:  {{created_at}}</li>
      <li>Bean type: {{beanType}}</li>
      <li>Rating: {{rating}}</li>
    </a>
  </ul>
  {{/each}}
  <button type="button" name="button"><a href="/brews/new/{{userId}}">New brew</a></button>
</section>
